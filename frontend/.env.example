# 前端API配置
# 如果前后端在同一台机器，使用相对路径（通过Vite代理）
# VITE_API_URL=/api/v1

# 如果前后端分离，设置实际的后端地址，例如：
# VITE_API_URL=http://192.168.31.38:8000/api/v1

# 后端代理地址（用于Vite开发服务器代理）
# 如果前后端在同一台机器，使用默认值即可
# VITE_BACKEND_URL=http://localhost:8000
